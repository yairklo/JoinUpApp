const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const game = await prisma.game.findUnique({ where: { id: 'cmplbkp4f000pks31pt3nm7d0' } }); console.log('Game:', game); const chat = await prisma.chatRoom.findUnique({ where: { id: 'cmplbkp4f000pks31pt3nm7d0' } }); console.log('Chat:', chat); const part = await prisma.chatParticipant.findMany({ where: { chatId: 'cmplbkp4f000pks31pt3nm7d0' } }); console.log('ChatParticipants:', part.length); } main().finally(() => prisma.$disconnect());
